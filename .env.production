# Production Environment Variables
# These should be set in Vercel dashboard under Project Settings > Environment Variables

# Frontend URL (automatically set by Vercel, but you can override)
VITE_API_URL=/api

# Backend environment (for FastAPI)
ENVIRONMENT=production

# Optional: Custom frontend URL if using custom domain
# FRONTEND_URL=https://your-custom-domain.com
