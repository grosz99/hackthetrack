# ============================================================================
# VERCEL SERVERLESS FUNCTION DEPENDENCIES
# Optimized to stay under 250MB limit
# ============================================================================

# Core FastAPI stack (minimal, required)
fastapi==0.115.6
pydantic==2.10.6
python-dotenv==1.0.1
python-multipart==0.0.20

# ASGI server - Vercel handles this, but mangum needed for Lambda compatibility
mangum==0.17.0

# AI/LLM (required for chat features)
anthropic==0.47.0
httpx==0.28.1

# Data processing (required for analytics)
pandas==2.2.3
numpy==1.26.4

# NOTE: scipy (80-100MB) removed - using numpy alternatives where possible
# If advanced stats needed, implement lightweight alternatives or lazy import

# NOTE: snowflake-connector-python (50-70MB) made optional
# Only import when Snowflake features are actually used
# snowflake-connector-python==3.6.0
# cryptography==41.0.7

# NOTE: uvicorn removed - Vercel provides its own ASGI server
# NOTE: requests removed - using httpx instead (already included via anthropic)
